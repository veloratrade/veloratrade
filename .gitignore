# real data & secrets - must never be public
api/storage/*.sqlite
api/storage/*.sqlite-*
api/storage/*.log
*.env
!api/.env.example
# داده واقعی کاربران هرگز نباید در مخزن عمومی باشد
_database/*.sql
!_database/schema.sql
*.dump
*.sql.gz

# بسته‌های ساخته‌شده
*.zip

# فایل‌های موقت
.DS_Store
Thumbs.db
__pycache__/
*.py[cod]

# context زنده و محلی هر جلسه — تولیدشده توسط tools/velora-status.sh --context
.session/
